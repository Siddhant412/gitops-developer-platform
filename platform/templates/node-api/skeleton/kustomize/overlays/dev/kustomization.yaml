apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev
resources:
  - ../../base
patches:
  - path: deployment-patch.yaml
images:
  - name: ghcr.io/${{ values.repoOwner | lower }}/${{ values.repoName | lower }}
    newTag: latest
