# Terraform Layout

Use this directory for shared platform modules and environment-specific Terraform stacks.

- `modules/` for reusable building blocks
- `environments/` for dev, staging, or cloud-specific roots
