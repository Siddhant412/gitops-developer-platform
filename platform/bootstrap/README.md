# Bootstrap Assets

This directory will hold local environment bootstrap assets.

- `kind/` for cluster creation and local registry wiring
- `argocd/` for Argo CD install and app-of-apps bootstrap
- `backstage/` for Backstage deployment manifests used outside local `yarn start`
